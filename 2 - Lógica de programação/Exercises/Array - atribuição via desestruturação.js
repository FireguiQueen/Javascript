// Considere o seguinte array.
// Atribua os valores do array às variáveis `a`, `b`, `c`, `d` e `e`.
const numbers = [1, 2, 3, 4, 5];
const [a, b, c, d, e] = numbers;
console.log(a, b, c, d, e);



// Considere o seguinte array
// Atribua os valores do array às variáveis `fruit1`, `fruit2`, `fruit3` e `fruit4`.
const fruits = ['apple', 'banana', 'orange', 'kiwi'];
const [fruit1, fruit2, fruit3, fruit4] = fruits;
console.log(fruit1, fruit2, fruit3);



// Considere o seguinte array
// Atribua o primeiro valor do array à variável `primaryColor` e o restante do array à variável `secondaryColors`.
const colors = ['red', 'blue', 'green'];
const [primaryColor, ...secondaryColors] = colors;
console.log(primaryColor, secondaryColors); 



// Considere o seguinte array:
// Atribua o primeiro valor do array à variável `x`, o segundo valor à variável `y` e o terceiro valor à variável `z`.
const coordinates = [10, 20, 30];
const [x, y, z] = coordinates;
console.log(x, y, z)



// Considere o seguinte array:
// Atribua os dois primeiros valores do array às variáveis `firstPerson` e `secondPerson`.
const names = ['Alice', 'Bob', 'Charlie', 'David', 'Eve'];
const [firstPerson, secondPerson] = names;
console.log(firstPerson, secondPerson)


// Considere o seguinte array:
// Atribua os três últimos valores do array a três variáveis diferentes.
const students = ['John', 'Jane', 'Alex', 'Emily', 'Michael'];

